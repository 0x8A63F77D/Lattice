Task 1: complete (commits 98aedb1..46335b0, review clean; Minor deferred: consider committing NuGet.Config for fresh-clone reproducibility; implementer had to add nuget.org source locally)
Task 2: complete (commits 46335b0..c8e2301, review clean; warnings resolved: InternalsVisibleTo + fixture copy verified in Task 1 diff)
Task 3: complete (commits c8e2301..73c11b7, review clean; Minor deferred: comment on post-terminator byte discard assumption; redundant double-dispose in DisposeAsync)
Task 4: complete (commits 73c11b7..6d972d8, review clean; Minor deferred: RawPayload 2000-char truncation vs doc-comment wording)
Task 5: complete (commits 6d972d8..9328fa0, review clean after fix; controller fixed missing using Xunit, removed stray commit.sh, narrowed CS1591 handling to pragma in Enums.cs + doc comments elsewhere; 0 warnings)
Task 6: complete (commits 9328fa0..b00b2d1, review clean)
Task 7: complete (commits b00b2d1..ca71d5a, review clean; Minor deferred: no fixture with unknown/extra elements interleaved in client_state)
Task 8: complete (commits ca71d5a..01775f0, review clean; pragma deviation was controller-sanctioned in dispatch)
Task 9: complete (commits 01775f0..3d63361, review clean)
Task 10: complete steps 1-3 (commits 3d63361..c8a9d65, review clean; step 4 manual gate pending)
Task 10 step 4: MANUAL GATE PASSED — real smoke test vs BOINC 8.2.11 (D:\BOINCData data dir), exit 0, SMOKE TEST PASSED; note: 0 projects attached, get_results real-data path unexercised
Task 11: complete (verification-only, no commit; pack OK, contents verified)
Final review: Ready to merge=Yes; applied minor fixes (UTF-8 password hash, framing comment, dispose doc); 53/53 green + smoke test re-passed

## M2a (plan 2026-07-06-m2a-guirpc-extensions.md, branch m2a-guirpc-extensions)
Task 1: complete (commits f4cb3dd..1e68e6d, review clean)
Task 2: complete (commits 1e68e6d..0adef97, review clean)
Task 3: complete (commits 0adef97..cfe3c87, review clean; warning resolved: gui_rpc_client_ops.cpp citation verified correct by controller; Minor deferred: BoolValue/DblValue near-duplicate ParseHelpers; test comment overstates GetTimestamp equivalence (seconds vs ms precision))
Task 4: complete (commits cfe3c87..afbf918, review clean; implementer test run hit transient 0x800711C7, controller verified 62/62 green)
Task 5: complete (commits afbf918..738a692, review clean)
Task 6: complete (commits 738a692..124b41d, review clean after fix; controller ordered BOINC_DATA_DIR env var instead of hardcoded D:\BOINCData; Minor deferred: env var value not trimmed before Path.Combine)
Final review (M2a): Ready to merge=Yes, all findings Minor (timestamp precision divergence, helper duplication, FinalElapsedTime/PlanClass only default-asserted, IGuiRpcClient omits State/DaemonVersion deliberately, proxy IP in plan doc pre-existing convention)
Task 7 fix: Codex P2 elapsed-time fallback fixed (both halves), 64/64 green
Task 7 (PR #6): Codex re-review P2 (resumed-bytes progress) REFUTED with daemon-source evidence (file_xfer.cpp: bytes_xferred initialized to starting_size/upload_offset, cumulative); reply posted on PR thread; awaiting user merge
- M2a MERGED to main (PR #6, 2026-07-06). Milestone complete.

## M2b — Lattice.Core (plan docs/superpowers/plans/2026-07-07-m2b-core.md, branch m2b-core, started 2026-07-07)
Task 1: complete (commits 9235b4e..e0404bb, review clean). Minor notes for final review: Save leaves orphaned .tmp if serialize throws; null-forgiving GetDirectoryName; Load TOCTOU (File.Exists then OpenRead).
Task 2: complete (commits e0404bb..0c38570, review clean, reviewer independently re-ran build+tests). Minor note for final review: Mutate mutates in-memory config before Save — divergence if Save throws.
Task 3: complete (commits 0c38570..d75d9bb, review clean, no findings; builder arg order verified against real records; TimeProvider.Testing 10.7.0 added).
Task 4: complete (commits d75d9bb..f50517e, review clean, reviewer independently re-ran build+tests). Implemented INLINE by controller during ~75min classifier outage (TDD red step skipped, noted in report). Minor deferred: no duplicate-key fixture test; O(projects x results) counting.
Task 5: complete (commits f50517e..4d6ced3, review clean after fix round 1). Two Important findings fixed in 4d6ced3: Status now Volatile.Read/Write on backing field; finally-block client dispose guarded with try/catch + new behavioral test Dispose_failure_in_client_does_not_stall_the_loop (red→green). Full suite 103/103, -warnaserror clean. Minor deferred for final review: RequestRefresh XML doc claims poll-tick-when-Connected which lands in Task 6 (make good or soften); DisposeAsync reads _loop without _gate (Start/Dispose interleave race, untested contract); bare catch{} on dispose guard swallows OCE (documented intent).
Task 6: complete (commits 4d6ced3..169b181, review clean after fix round 1). Important fixed in 169b181: re-auth busy loop bounded — one silent re-auth per successful tick, second consecutive unauthorized propagates to Retrying/backoff; new test Mid_poll_repeated_unauthorized_after_reauth_backs_off_instead_of_spinning (genuine red hang→green). Controller verified full Release suite 111/111 + -warnaserror clean (fixer's runs were Debug due to transient Smart App Control block on Release testhost after killing the hung red test; block self-cleared, verified gone). Minor deferred for final review: unguarded SnapshotUpdated/MessagesAdded handler exceptions escalate to Retrying (consistent with StatusChanged pattern); no test for message seqno continuity across real reconnect (correct by inspection); per-tick HashSet rebuild in TickAsync (plan-mandated, perf pass later). ENV NOTE for user: Smart App Control briefly blocked Release-mode testhost loading Lattice.Core.dll (0x800711C7); self-resolved, but may recur when test processes are killed mid-run.
Task 7: complete (commits 169b181..9de956a, review clean, no fixes; both files verified byte-for-byte vs brief; 115/115 Release green, -warnaserror clean). Minor deferred for final review (all plan-mandated): fire-and-forget removed.DisposeAsync() on HostRemoved has no fault observation (latent if DisposeAsync ever throws); ctor gap between monitor population and Changed subscription relies on HostRegistry's documented single-threaded contract; no IntervalChanged propagation test.
Task 8: complete (commits 9de956a..a1eda1b, review clean, no fixes; verbatim transcription verified; live smoke PASSED vs real daemon — full Connecting→Authorizing→FetchingState→Connected walk, 313 messages buffered, exit 0; 0 tasks/projects on this machine so app= join criterion unexercised in real data (covered by fake tests; attach-a-project follow-up still open); no password in output, LastError paths independently verified password-free). Minor deferred: csproj backslash vs forward-slash path inconsistency.
Final review (M2b): Ready to merge=Yes (opus, package 03c9466..a1eda1b + fix verification a1eda1b..8f70002). One Important fixed in 8f70002: pre-Connected BoincUnauthorizedException (auth1 remote refusal / exchange_versions / get_state) now → terminal AuthFailed via new catch; internal HostSessionLostException preserves mid-poll escalation to Retrying. Controller decision (surfaced in PR): message cursor reset + MessageLog.Clear() on each new connection (official-Manager semantics; placed after GetStateAsync success, before Connected — buffer survives failed retries; daemon-restart test added). Also in 8f70002: SnapshotBuilder duplicate-key characterization test, teardown fault observation + IntervalChanged test, csproj slash fix. 121/121 Release x3, -warnaserror clean. Minor deferred to M2c: reconnect test wait condition could be airtight (Messages.Count==2); HostSessionLostException LastError wording could confuse (says unauthorized while Retrying); plus earlier triaged defers (Task 1 tmp/TOCTOU, Task 2 Mutate-before-Save, subscriber-exception escalation, per-tick HashSet, ctor gap).
PR #7 OPENED (2026-07-07, https://github.com/0x8A63F77D/Lattice/pull/7): branch pushed, controller re-verified 121/121 + -warnaserror before push.
Codex round 1 (reviewed 8f70002): 4 findings, ALL verified real and fixed (125/125 x3, pushed, in-thread replies posted): P1 world-readable config w/ passwords on Unix → 2a4f72f (UnixCreateMode UserRead|UserWrite + 700 dir, Windows keeps File.Create; test asserts on ubuntu CI leg — passed); P2 Mutate swaps before Save → 02a7acb (save candidate first; = our deferred Task 2 minor, Codex escalated); P2 stale snapshot after UpdateConfig + P2 in-flight RPCs not canceled → 3e915f5 (per-connection linked CTS canceled by UpdateConfig; bare-OCE catch = config-change reconnect no backoff; _configChanged checks before Connected and before snapshot publish; FakeGuiRpcClient hooks now honor ct via Task.WaitAsync). CI green post-push.
CI matrix widened to ubuntu+windows+macos (f18c7f9, fail-fast off, user-requested); ruleset protect-main now requires all three legs (user authorized the API update). All legs green.
Codex round 2 (reviewed 3e915f5): 2 P2 findings, both verified real, fixed in 7265f0f (127/127 x3; red→green obtained by temporarily reverting the fix): TickAsync stale-message publish window (guard now also before message append/MessagesAdded, pre-snapshot guard kept) and client left connected while parked in AuthFailed (park moved out of catch, finally disposes before WaitForConfigChangeAsync — BOINC daemons allow few GUI RPC connections). In-thread replies posted, 3-leg CI green.
ENV: Smart App Control recurred during round-2 fix (blocked GuiRpc.dll load in all processes, self-cleared again) — second occurrence, flag to user if it recurs.
Codex round 3 (reviewed 7265f0f): 1 P2, verified real, fixed in e75c47f (128/128 x2, red→green): failed client held open through Retrying backoff (up to 60s) — backoff wait moved after finally-dispose, same restructure as the AuthFailed park. Controller then swept the whole file for both defect classes (waits-with-live-client: only remaining is the healthy steady-state interval wait, by design; publish-after-config-change: all three surfaces guarded) — no fourth instance. In-thread reply posted, 3-leg CI green. Deferred note for M2c: consider restructuring RunAsync into connection-attempt-returns-outcome shape so the dispose-before-wait invariant is structural, not per-exit (discussed with user; agreed as M2c-adjacent standalone refactor with existing tests as safety net).
Codex rounds 4-9 (all triggered by Claude via `gh pr comment` after round 4; user granted merge authority with severity judgment delegated): r4 stale-tmp-perms + factory-throw fixed (d830840) + controller-sweep subscriber guards (ab11d9a); ROOT-CAUSE RESTRUCTURE 97dfb74 (RunAttemptAsync scoping, opus, user-prompted "stop monkey-patching" — see memory repeated-patches-mean-wrong-fix); r5 Load normalization (88c19b6); r6 restructure regression #1 attempt-reset (9b594f3); r7 restructure regression #2 snapshot/CTS lock atomicity (2f5016e); r8 post-Build publish recheck (86f65b0, generation-machinery deliberately declined — snapshot retains last-known data by design).
**M2b MERGED to main (PR #7, merge commit 61c98c5, 2026-07-08, 139 tests).** Round-9 findings judged non-serious and deferred with in-thread rationale: P2 _daemonVersion stamped from unaccepted attempts (display vintage only — fix in M2c: keep attempt-local until Connected publish); P3 Save with bare filename throws on CreateDirectory("") (outside app usage).
M2c FOLLOW-UPS carried from this branch: the two round-9 deferrals; RequestRefresh/poll-tick doc nit resolved by Task 6; reconnect-test wait-condition polish; HostSessionLostException LastError wording; Task 1/2 config minors (tmp orphan now moot after r4 delete-first, Mutate divergence fixed r1C2); attach a BOINC project + re-run smoke for real get_results data; consider committing NuGet.Config.
NEXT: M2c (Avalonia shell, 5 views per docs/design/m2/; DataGrid vs TreeDataGrid decision at planning with Avalonia MCP).
NOTE: all briefs for Tasks 4-8 were hand-written by controller during classifier outage (task-brief script unavailable); they are verbatim plan extracts. Implementer dispatch prompt template: see any task above — haiku for implementers (transcription), sonnet for task reviewers, opus for final review. Trailer on commits: Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>

## HostMonitor verification (plan docs/superpowers/plans/2026-07-08-hostmonitor-verification.md, branch hostmonitor-verification, PR #8, started 2026-07-08)
Task 1: complete (commits cbed6d9..b4db199, review approved; Important fixed by controller in b4db199: fsproj missing WarningLevel 5 — plan template's own omission, level-5 build verified clean; Model.fs verified byte-for-byte vs brief by reviewer)
Task 2: complete (commits b4db199..aa6fc1d, review approved after controller-adjudicated fixes; RED-FIRST PAYOFF: mutant discipline exposed 3 plan-level defects — M1 mutant was semantic no-op (corrected to lost-notification split), PublishConnected attempt=0 unfaithful to HostMonitor.cs:319 and masked M2, vintage fields needed per-attempt reset to stop I1m false alarms on healthy model; Promela plan text mirror-fixed in 29a695f before Task 4 brief cut; 12/12 + 139/139 green, warnaserror clean; Minor deferred: NoComparison attribute lacks comment)
Task 3: complete (commits aa6fc1d..acc570d + style edf6aef, review approved after 2 controller-adjudicated fix rounds; RED-FIRST PAYOFF x2: (1) history-variable L1 encoding UNSOUND vs sticky-latch WaitAsync (delay can win WhenAny with latch completed — correct behavior; entry-consume at WaitAsync:183-187 now transcribed into model at SnapPublish/RetryDecide/park entries; L1 recast as liveness "wake leads-to consumed-or-dead"); (2) implementer's M6 STOP exposed checker defect: full-graph bottom SCCs let always-available EnvDispose discharge every liveness obligation — fairness now scoped to loop-only edge subgraph per spec "weak fairness on Loop"; same defect fixed in pml as env stutter branch + plain-Idle LTL goals (dca75b1, ec57b5b); implementer's unfaithful rescue edits (DelayFires wake gate, env gating post-dispose, configChanged clear on dispose) reverted; missing L3 test added (was silently skipped); healthy 27,510 states / 3,324 bottom loop-SCCs all obligation-free; M6 204 pending states caught; 18/18 + 139/139, warnaserror clean. Functional-idiom controller pass edf6aef: shared loopSuccs (drift risk), M2 Seq.exists; throw-style iteration kept as checkInvariant-uniform idiom. ENV: Smart App Control blocked fresh test DLL hashes mid-task (CodeIntegrity 3077, 3rd occurrence this project) — flag to user if recurs.)
Task 4: complete (commits edf6aef..e9dab9c, review approved after 1 fix round; Critical caught by reviewer: brief-mandated PERMANENT I1m backstop assert missing from monitor() — MsgP assert provably cannot catch eager Fetch-time logVintage write (reachedConnected already true by MsgP); added in e9dab9c, verified compatible with 29a695f vintage-reset semantics (no healthy false-alarm window); also fixed: stale -q script comment, report misattribution (controller dispatch text labeled as brief quote), false no-deviations claim, line counts; pml byte-for-byte vs brief otherwise; step 3 red-check DEFERRED to post-Task-5 CI (no local spin) — MUST run before ruleset gains model-check as required; NOTE for Task 11 README: pml I5 assert is a deliberate slot (faulted never set in pml) — lifecycle-fault class owned by F# M5 + C# harness A6; NOTE: pml authRefused flag vs F# attempt=-1 sentinel is deliberate N-version divergence)
Task 5: complete (controller-executed; commits 9d0ab57 ci job + c0bc5d7 script fix; PLAN DEFECT #7 found by first CI run: 'spin -a -N NAME' reads a never-claim FILE, not a named ltl — script now compiles pan once and selects claims at run time (./pan -N), safety sweep runs -noclaim; healthy model GREEN all 4 runs (safety+L1+L2+L3, errors: 0 each, run 28935595137); Task 4's deferred red-check DONE on CI: break1 eager-log-write RED via the I1m backstop assert that Task 4's reviewer forced in (depth 43, run 28935735725, commit b45662c reverted c10c6d2), break2 snapshot-split RED via monitor I3 (depth 57, run 28935863913, commit 2c6bef7 reverted 56410bd), green after reverts (run 28935947008); ruleset protect-main 18505416 now requires 4 contexts incl model-check (PUT via powershell no-BOM payload — jq/py absent on this box, python -c exit 49); NOTE Task 11: pml line ~43 has leftover empty '/* L1 history */' comment — clean with README task)
Task 6: complete (commits 56410bd..b0a168b, review approved after 1 fix round; Important caught by reviewer: TickBeforeMsgPublish/TickBeforeBuild probes placed INSIDE the guard pair instead of after it — would have made the A1 guard-to-publish sweep vacuous (probe before guard = UpdateConfig gets caught by guard, no window to pin); moved after guards in b0a168b, consistent with TickBeforeSnapPublish; 15 probe points + ProbeController verified against brief, no probe under _gate (deadlock-free), unattached fast path allocation-free; sync-rule statement in both commit messages; 140/140 + 18/18, warnaserror clean)
-- MACHINE MIGRATION (2026-07-08): execution resumed on macOS (Darwin 25.5, .NET 10.0.301); baseline re-verified on this box before Task 7 (140+18 green, -warnaserror clean). Git identity was auto-generated on first commit (bogus hostname email) — fixed with repo-local user.email + --reset-author. Smart App Control notes above are Windows-only, moot here.
Task 7: complete (commits b0a168b..2ff2cba, review fix round 1; production fix verbatim per plan, reviewer verified red empirically vs b0a168b (expected 8.0, got 9.9); Important fixed by controller: implementer's test deviated from plan script — used no-op UpdateConfig reconnect which produces only ONE Retrying event (ConfigChanged outcome never publishes Retrying), asserted [0] instead of the second event, and never exercised the post-Connected failure path; controller rewrote to literal script (failResults toggle → Retrying #1, reconnect 9.9 + get_state throw → Retrying #2, assert ElementAt(1)) reusing HostMonitorStateMachineTests failPoll+RequestRefresh idiom, removed raw Task.Delay(50); controller re-verified red on rewritten test vs b0a168b then 141+18 green, -warnaserror clean; amended into the single task commit)
-- PROCESS NOTE: superpowers:subagent-driven-development skill loaded mid-Task-8 (was unavailable earlier this session); two-stage review (spec then quality) in effect from Task 8 onward.
Task 8: complete (commits 2ff2cba..0dc8c80, spec review round 1 FIX REQUIRED then APPROVED; quality review APPROVED). Production diff approved unchanged from first round (ReplaceAll one-lock swap; _lastSeqno field deleted; PollAsync-local cursor + firstTick; TickAsync returns (state, lastSeqno); probes/guards preserved; Clear() deleted — zero callers). CRITICAL CATCH by spec reviewer: implementer's two tests were FALSE GREENS — empirically passed 10/10 against pre-fix 2ff2cba src (implementer's claimed red evidence did not reproduce; weak tests sampled state only after successful reconnect whose first tick refills the log). Haiku implementer session lost to infra error; fix escalated DIRECTLY to opus per model policy: tests rewritten with freeze idiom (BeforeAcceptGuard armed post-first-Connected + second UpdateConfig mid-freeze + abort; TickBeforeMsgPublish armed while parked in Retrying + seqno-reset single-message daemon restart shape), load-bearing assertions taken WHILE FROZEN, try/finally Release so red fails clean instead of hanging disposal (ProbeController one-shot arm verified). Red re-verified empirically by spec reviewer: both fail Expected 2/Actual 0 at frozen assertions (lines 134/227), no hangs. Stale _lastSeqno comment in HostMonitorPollingTests reworded. 143+18 green, -warnaserror clean. Minor deferred to Task 10/final: test setup duplication (~15-20 lines, factor helper when sweep matrix lands), MessagesAdded raise duplicated across replace/append arms, ReplaceAll vs Append trim idiom divergence, test naming convention mix (PascalCase vs snake_case).
Task 9: complete (commits 0dc8c80..2a22289, spec review APPROVED no findings; quality review 1 Important fixed by controller in amend — DisposeAsync first-gating mechanism comment added per file idiom, doc-only). Production fix verbatim per plan (_disposed under _gate; Start captures token under lock, lambda never reads _cts.Token; DisposeAsync test-and-set + unconditional await of lock-read loop task). Red empirically verified by reviewer: all 3 LifecycleTests fail pre-fix with ObjectDisposedException at _cts.Cancel(); reviewer adjudicated DisposeWithoutStartThenStartIsInert's Disconnected assertion as non-discriminating pre/post fix (faulted task never touches Status) — plan-sanctioned, red carried by second DisposeAsync; reviewer also exhaustively traced fixed-code interleavings (no hole; empirically confirmed token-after-source-dispose polling is safe in .NET 10) and verified disposeFlag pre-exists in Model.fs + pml (model-sync claim substantiated). 146+18 green, -warnaserror clean. Minor deferred to Task 10: switch DisposeWithoutStartThenStartIsInert's Task.Delay(50) proxy to internal _loop.IsFaulted check when A5 arm makes _loop internal; Config() helper duplication → TestData.MakeHostConfig consolidation (3rd copy imminent in ReentrancyTests); _disposed field-level comment (matches _started precedent, informational only).
Task 10: complete (commits 2a22289..ee624f9 + cleanup a87afd4; opus implementer per model policy; spec review APPROVED, quality review APPROVED all-Minor). 45-case sweep theory (15 points x 3 actions) green FIRST RUN — no pre-existing bugs surfaced, riders A/B/C hold; + 2 reentrancy pins; 193+18 green, interleaving suite 3x stable (implementer) + 2x (controller). Design: generation stamping via DaemonVersion=(8,gen,0)/HostName/message Body (payload-derived, no side channel); stale-gen connects park forever as structural backstop (reviewer confirmed unreachable under current sequencing — comment fixed in a87afd4); doctrine encoded one case-arm per row. Special cases (all reviewer-adjudicated sound): FinallyEnter/AfterCtsDispose need failing-connect preamble (finally unreachable happy-path); BeforeParkWait x RequestRefresh asserts L3b parked-stays-parked via yield-driven bounded settle (time.Advance inert — park never reads TimeProvider; comment fixed) + UpdateConfig revival; PollAfterWait refresh case needs 3 ticks (sticky-wake preamble). Fold-ins landed: TestData.MakeHostConfig (all 5 test files now delegate after a87afd4); _loop internal (visibility-only, commit-message sync note); LifecycleTests Task.Delay(50)→_loop sentinel assertions. Deferred informational: LifecycleTests redundant IsFaulted assert (names the A5 property, kept deliberately); _loop underscore naming vs internal-member convention (cosmetic).
Task 11: complete (commit b097621 — verification/README.md all 7 sections + dangling L1-history pml comment removed, comment-only; sonnet implementer verified cross-ref names against actual artifacts, expanded sketch shorthand to real helper/test names; 18-field shared-state inventory with vintage/visibility justifications + _lastSeqno deletion note).
FINAL REVIEW (opus, whole branch 61c98c5..HEAD): READY TO MERGE. Verified: cross-ref table accurate vs Properties.fs/pml/harness; inventory complete/correct (all 18 fields, regimes checked against Start/DisposeAsync/UpdateConfig); riders MATCH models (daemonVersion only at Connected publish; log mutation only post-guard with first-tick replace; Start/DisposeAsync vs disposeFlag) — no divergence; probe placement sound (none in lock, moved points still post-guard after rider B's TickAsync rewrite, null path allocation-free); commit sync statements present on all six session commits. One LOW finding fixed by controller in amend: README cited pre-amend SHA f9f3d64 → 0dc8c80 (b097621). Controller-run gate: 193+18 green, -warnaserror clean.
Red-first evidence ledger (session commits): rider A red = expected 8.0 got 9.9 vs b0a168b pre-fix; rider B red = Expected 2/Actual 0 at frozen assertions (SweepTests lines 134/227) vs 2ff2cba pre-fix, empirically re-verified by spec reviewer after false-green catch; rider C red = ObjectDisposedException at _cts.Cancel() x3 tests vs 0dc8c80 pre-fix. Explorer state count: 27,510 healthy states (Task 3, unchanged bounds). Pan stats: see Task 5 entry (runs 28935595137/28935735725/28935863913/28935947008).
Pushed edc7a8a; all 4 CI legs green first run; PR #8 body updated (Implementation section, two behavior changes flagged); Codex round on edc7a8a: "Didn't find any major issues. Swish!" — zero findings, no fix round needed.
**HostMonitor verification MERGED to main (PR #8, merge commit 6fd484b, 2026-07-08, 193+18 tests).** Post-merge: local main verified green.
NEXT: M2c (Avalonia shell, 5 views per docs/design/m2/; DataGrid vs TreeDataGrid decision at planning with Avalonia MCP). Carried follow-ups: M2c list at line 45 (minus items resolved by this branch: daemon-version vintage fixed by rider A; RunAsync restructure obviated by verified invariants); attach a BOINC project + re-run smoke (needs a BOINC daemon on the new macOS machine — none verified yet); consider committing NuGet.Config.
PARALLEL TRACK (user decision 2026-07-08): the deferred F# functional-core restructure of HostMonitor (pure F# transition function grown from tests/Lattice.Verification/Model.fs as the real core; C# thin async I/O shell; see memory fsharp-for-functional-domains) runs in a SEPARATE session, spawned via task chip. M2c planning continues in this session. Coordination: HostMonitor public surface must stay frozen for M2c; whichever branch lands first, the other rebases; uncommitted .mcp.json + this ledger ride with whichever branch commits first.
2026-07-08 M2c planning session: Avalonia Build MCP registered via project .mcp.json (avalonia-docs, free HTTP endpoint). KEY FINDING: TreeDataGrid is now Avalonia Pro (PAID) per current official docs; free MIT package tops out at Avalonia 11.x — DECISION: all M2c views use free Avalonia.Controls.DataGrid (separate NuGet + theme StyleInclude); Projects two-level hierarchy via a DataGrid-compatible mechanism to be chosen in the spec (flattened rows w/ chevron toggle, grouping, or Expander list — design README already sanctions the alternative).
ORDERING UPDATE (user decision 2026-07-08, supersedes PARALLEL TRACK note above): F# functional-core restructure of HostMonitor runs FIRST in its own session; M2c design is PENDING until that round completes. M2c planning session stopped after: avalonia-docs MCP registered (.mcp.json, untracked), TreeDataGrid-is-paid finding + all-views-use-free-DataGrid decision, scope-split question deliberately unanswered. The F# session takes the uncommitted ledger + .mcp.json with its first commit.

## HostMonitor functional-core restructure (branch claude/intelligent-lumiere-4110d4, session start 2026-07-08)

Scope: memory fsharp-for-functional-domains decision — pure F# HostMachine transition core (production), C# HostMonitor reduced to interpreter shell; verification Model.fs becomes a shell model over the PRODUCTION step. Spec: docs/superpowers/specs/2026-07-08-hostmonitor-functional-core-design.md (judgment calls §9 pending user review at PR time).
Coordination: this branch TOOK the ride-along .mcp.json + this ledger's uncommitted edits (first-to-branch rule) — M2c session must NOT re-commit them; HostMonitor public surface frozen for M2c; later-landing branch rebases.

Task 1: complete (90040f8 — Lattice.Core.Machine scaffold + HostConnectionState move; quality fixups: IsPackable/GenerateDocumentationFile/redundant WarningsAsErrors).
Task 2: complete (b02140f — HostMachine pure core + 16 MachinePins; opus adversarial review found 2 controller design bugs: reauth-allowance never reset (fix: tick-completion triple at PostBuildGuard, NOT reviewer-suggested TickFetched which would hammer-loop) + refetch-unauthorized parked instead of silent-reauth (fix: shared unauthorizedMidPoll fold); red-first: 4 pins red pre-fix, empirically re-verified post-fix by reviewer).
Task 3: complete (c5afdcf — Model.fs = shell model over PRODUCTION step; 395,659 states (14.4x old 27,510: per-command granularity + queue in state); all 7 mutants detected, M2/M6 empirically re-verified via property-weakening; new coverage: Reauthorizing/Refetching/hasPassword branches; deviations from design table accepted: outer-first cancel checks, EffectOk-on-batch-end).
Task 4: complete (this commit — HostMonitor.cs interpreter shell, +301/-336; KEEP-list byte-identical except BackoffDelay delegation; zero F#-side interop sugar needed; opus review approved, all 15 probe positions verified).
Task 5: this commit (README §§1-3/6-7, pml comments, CLAUDE.md, 2 stale HostMonitor comments).
Controller judgment calls (for PR): O-prefixed Outcome constructors (FailureKind name collision); long single-line OR-pattern in fault fold (F# parser); defensive exitLoop on unexpected (phase,input); machine surface public-but-not-API; controller idiom reviews done for Tasks 2/3 (no mutable, no imperative loops outside Explorer kernels + Properties edge-walks, both pre-existing style).
Task 6: complete — PR #9 (https://github.com/0x8A63F77D/Lattice/pull/9), branch pushed, all 4 CI legs green first run (incl. model-check: pml comment-only confirmed by Spin), Codex round on cfe1435: "Didn't find any major issues. Breezy!" — zero findings.
**Functional-core restructure MERGED to main (PR #9, merge commit a185e49, 2026-07-09, 193+36 tests).** User signed off spec §9 judgment calls via merge. Post-merge: M2c planning session verified local main green (193+36, 已通过!) and resumed M2c design from the recorded pause point.
NEXT: M2c (Avalonia shell, 5 views per docs/design/m2/; all views on free Avalonia.Controls.DataGrid — TreeDataGrid is Avalonia Pro/paid; scope-split question open at pause). NOTE for M2c: HostMachine.Phase changes trigger verification/README.md correspondence rule 4 (same-commit I6 projection-table extension, domain-shaped names preferred).
Red-first evidence ledger: Task 2 machine bugs — 4 pins red pre-fix (firstTick at MachinePins.fs:73, reauth-reset, 2x refetch-unauth), reviewer empirically re-verified by reverting one hunk (1 failed/33 passed) then restore (34 green). Task 3 mutants — M2/M6 property-weakening check: both tests fail when their predicate is a tautology, restored green. Explorer: 395,659 states / 34,269 bottom loop-SCCs.
Post-Codex addition (user review insight 2026-07-09, "the model should BE a Connection State"): I6 phase/status coherence property — the machine projected onto the published status IS the 7-value HostConnectionState lifecycle. F# invariant (trajectory-dependent phases Dispatch/SnapshotWait/TearingDown excluded; pending-publish batches excluded) + M8 red-first witness (backoff publish lies Connected) + pml d_step asserts (mapping one phase later than F# — publishes fire ON transitions in pml; declared N-version divergence) + HostMachine.fs conceptual-model doc (pure Mealy machine; projection table) + README I6 row. No production semantics touched.
Correspondence rule 4 added (README §2): domain-vocabulary discipline — phase changes must extend the I6 projection table in the same commit; operational phase names are recorded debt, domain-shaped names preferred. Controller owns this smell-guard per user delegation 2026-07-09.

## M2c-1 — Avalonia shell stage (branch m2c-shell, 2026-07-09 onward)

Scope: NavigationView shell, 5 views (TaskList, Projects, Transfers, Messages, Settings), binding/marshaling, HostStore integration. Two-stage review enforced (spec then quality); all 16 tasks red-first verified; controller live rehearsal gate 2026-07-10 vs real BOINC 8.2.11 localhost daemon.

Task 1: complete (6ba1ef8 — Lattice.App scaffold; Avalonia 12.1, FluentAvaloniaUI 3.0; quality review clean).
Task 2: complete (a4b3486, 3701278, 990bbed, b9799b8 — headless tests; Avalonia.Headless.XUnit 12.0.5 requires xunit v3 — DEVIATION from plan 2.9.3; shared test fakes extracted to new xunit-free `tests/Lattice.TestSupport` lib (namespace preserved `Lattice.Tests`); Wait.cs Assert.Fail→TimeoutException; mixed xunit v2 (Tests) + v3 (App.Tests) permanent; quality review clean).
Task 3: complete (e05553a, cc3b8e9 — token definitions + icon folder generation; DEVIATION: icon "Task List Square" renamed "Task List Square LTR"; review fixes: generator atomicity (mktemp+mv), dead-code guard, +3 brush-token test rows).
Tasks 4–6: complete (ae56091, 0494796, 0c2872f — infra seams INotifyPropertyChanged/event plumbing + settings registry; quality reviews clean; transcription verbatim).
Task 7: complete (3632f99, 3c72043 — HostStore polling scheduler + concurrency; review fixes: dispose-guard for queued posts (deterministic UI-thread flag), marshaling-contract pin test, Debug.Assert(CheckAccess) in registry handler).
Task 8: complete (f0dab55, e4e8bdf — rail projection StateMachine; review fixes: explicit state cases + throw on unknown, totality test, leaner fixture; InternalsVisibleTo added).
Task 9: complete (f6d64de, 650ecc0 — ShellViewModel binding + subscription lifecycle; review fixes: rail-item ctor exception safety (Refresh before Tick subscribe), dispose-on-removal pinned via ManualUiClock.SubscriberCount).
Task 10: complete (6a180ee, b6d1e78 — ShellWindow XAML + input binding; DEVIATION: FluentAvalonia v3 renames (FANavigationView etc.), Ctrl+D1..4 gestures, explicit RenderTransform for spinner; review fixes: initial nav highlight + VM→view selection sync + conditional spinner class (Classes.spin binding), 3 sync tests).
Interlude (e0dbd1f): Avalonia.Diagnostics reference dropped — no 12.x package exists on nuget.org (Debug builds broken solution-wide).
Task 11: complete (fd19330, 17c9633 — SettingsViewModel connection test + dialog marshaling; review fixes: connection timeout 10s (OCE→"Connection timed out."), stale-result clear, Name null-guard, +2 tests).
Task 12: complete (285c7ee, c1e2f19 — SettingsView host list + remove flow; DEVIATION: FluentAvalonia v3 SettingsExpander/InfoBar/ContentDialog, Watermark→PlaceholderText (Avalonia 12 obsolete); real crash caught by review: double-click Remove stacked two dialogs→double RemoveHost→unhandled ArgumentException; fixed single-flight guard + existence check + 3 headless dialog-flow tests (red→green verified)).
Task 13: complete (15f2a68, c49c6a9 — AddHostDialog + password field; CanAddNow fallback for IsPrimaryButtonEnabled; FADeferral usage; review found SECOND real crash: FAContentDialog subclass StyleKey→template never resolved→KeyNotFoundException on first open; fixed StyleKeyOverride + AddHost persistence-failure guard; dialog-flow tests (red→green verified)).
Task 14: complete (e66a552 — composition wiring + link tests; pin test caught real bug: ExpandHost showed stale card (HasAuthError unset) during reconcile race; fixed RefreshFromEntry inside ExpandHost).
Task 15: complete (c78adaf, ae2519e — composition root LatticeApplication + bootstrapping; UnauthorizedAccessException added to config fallback filter; LatticeConfig.Default used).
Final counts: 342 tests green (193 Lattice.Tests + 113 Lattice.App.Tests + 36 Lattice.Verification), build clean under -warnaserror, Debug and Release both restore/build.

Live rehearsal (controller gate, 2026-07-10, real BOINC 8.2.11 localhost:31416): dotnet run fails (RenderTimer -6661 WindowServer attachment, 5 crash reports same cause); built binary via LaunchServices (open) runs stably on desktop session. Config pipeline proven: config.json (macOS SpecialFolder.ApplicationData → ~/Library/Application Support/Lattice) → registry → manager → monitor → TCP ESTABLISHED. AuthFailed path: wrong password → terminal AuthFailed, no hung connection, stable. Retrying path: closed port → backoff loop 0.0% CPU, no crash. Corrupt-config fallback: file→config.json.corrupt-<timestamp>, clean restart. NOT rehearsed (deferred to user demo): visual walkthrough (CTA, dialog, Connected + task count, rail visuals, settings round-trip, theme fidelity). Rehearsal artifacts cleaned; machine in first-run state for user demo.

Deferred to M2c-2: selected-view filled-icon swap; "All hosts" rail entry + aggregate subtext; Event-log InfoBadge; responsive column breakpoints; motion/reduce-motion; Mica verification on Windows; MessagesAdded consumption in HostStore.

Follow-ups accumulated (tracked for next touch):
- HostConnectionState.fs doc comment: Unreachable tier begins attempt >= 5 (comment) vs >= 4 (spec §9 + shipped projection). Comment-only fix, file in M2c-guarded dir — defer to dedicated commit outside M2c stage.
- generate-icons.sh: chmod 644 pre-mv (mktemp creates 600) + trap rm -f for temp file — next script touch.
- Password fields (SettingsView, AddHostDialog): TextBoxAutomationPeer exposes Text via UI Automation regardless of PasswordChar; Avalonia has no PasswordBox. Platform limitation accepted, comments in both files.
- Registry-mutation errors: RESOLVED in Codex round — RegistryGuard.TryMutate centralizes the IO/UnauthorizedAccess catch-set; all four UI mutation sites (AddHost, Save/UpdateHost, Remove, SetPollingInterval) route through it with inline surfacing (dialog InfoBar / card ValidationError / polling error line). 4 failure-path tests; Save guard falsified red by stripping it.
- HostStore raises Changed on IntervalChanged with empty cache (cosmetic; harmless re-render).
- xunit v2/v3 split permanent until Lattice.Tests migrates to v3 (optional, not urgent).

Post-PR user-demo feedback round (2026-07-10):
- Positive path verified by controller with user-granted permission: SmokeTest protocol + --core modes PASSED vs live daemon 8.2.11 (auth, Connected, snapshot); persisted config.json password byte-identical to gui_rpc_auth.cfg; user account confirmed in boinc_master group (file group-readable).
- TextBox text sat ~3-4px above optical center (user-reported, macOS). Root cause chain: Avalonia centers text mathematically not optically (AvaloniaUI/Avalonia#10658, open); FluentAvalonia's TextVerticalAlignmentOverride (default EnabledNonWindows, active — injects TextControlThemePadding 10,5,6,5) corrects CheckBox/RadioButton/ComboBox but NOT TextBox, which keeps VerticalContentAlignment=Stretch → glyphs top-align in stretched line box. Fix: app-level TextBox style VerticalContentAlignment=Center (community-standard per Avalonia discussions #13415/#10317; same technique FA uses for covered controls). Verified by headless-Skia pixel measurement: bias +4.0px → within ±1px. Candidate upstream issue for amwx/FluentAvalonia (measurement data + A/B renders archived in session; filing needs user approval).
- Visual/optical regression testing: deferred by user decision (排期). When scheduled: promote the throwaway headless+Skia render probe (HeadlessUnitTestSession + CaptureRenderedFrame + ink-gap measurement) into a Visual-category test leg; note HeadlessUnitTestSession.Dispose hangs after Dispatch returns (watchdog Environment.Exit workaround needed).
- i18n thread (parked 2026-07-10, user asked; DECIDE AT M2c-2 PLANNING): no provision exists anywhere (design/plan/code audited — UI strings hardcoded in XAML plus composed in VMs: status text, error messages, TimeText, dialog-title interpolations; protocol layer already InvariantCulture-clean). Full i18n now = overkill; zero seam = retrofit cost doubles once M2c-2 stamps 4 more views. Cheap seam recommended: while stamping M2c-2 views, route every user-facing string through one static Strings class (plain C# consts/format strings, no resx yet), XAML via x:Static — converts a future retrofit into swapping one class's backing store. BOINC community is international; post-1.0 translation requests are likely.
- UI automation testing thread (parked 2026-07-10, user asked to keep a marker): four tiers assessed. (1) Component-level headless — built, 119 tests, Avalonia's official path. (2) Journey-level in-process — RECOMMENDED NEXT: same infra, full composition root + fake RPC clients, one test per key user journey (add host → rail → auth-fail → settings linkage → persistence); natural fit when M2c-2 stamps out views; today the composition root is only covered by live rehearsal. (3) Visual regression — see previous bullet. (4) Cross-process E2E (Appium over UIA/NSAccessibility, as Avalonia.IntegrationTests.Appium does) — poor ROI at this project size, flaky, and rides the accessibility layer where our password-exposure peer limitation lives; re-evaluate at M4/release. Until then the user's manual demo gate is the E2E.
- Codex Review round (reviewed a9f680c, 2 × P2): (1) missing "All hosts" rail entry — deliberate M2c-2 deferral (see Deferred list), replied on PR; (2) Save persistence failure escapes command path — CONFIRMED, third unguarded same-class site ⇒ centralized per repeated-patches rule into Infrastructure/RegistryGuard (see resolved follow-up above).
- CI flake fixed (macOS leg, intermittent 2/5 across 7acc729..775b990): Confirming_remove failed because SettleDialogAsync ("sleep 300 ms then RunJobs") raced FAContentDialog's internal Task.Delay(200) close path on loaded runners. Replaced all wall-clock settles with Headless/HeadlessSync.WaitUntilAsync (poll the observable outcome — registry count / dialog gone from tree / error surfaced — 5 s ceiling only bounds a hung dialog); both per-file SettleDialogAsync helpers deleted. This was the final review's deferred "300 ms wall-clock coupling" hygiene note, pulled forward by CI. Remaining hygiene note still deferred: Layout(window) helper duplication across headless test files.
- Codex re-review round 3 (reviewed 3e20709, 1 × P3): SettingsView leaks a subscribed view per Settings visit — REFUTED empirically. Pin test replicating the shell's ContentControl+DataTemplates navigation shows Avalonia clears/rebinds the discarded child's DataContext, so the existing OnDataContextChanged unsubscribe runs; VM-side observable (HasRemoveSubscribersForTests) proves the handler itself is gone, and stripping the `-=` turns the test red (sensitivity verified). Test kept as a contract pin against future Avalonia behavior changes; no production change.
- Codex re-review (reviewed 787a91f, 1 × P2): config fallback could overwrite an unreadable-but-valid config — CONFIRMED (introduced by ae2519e widening the catch). Fix: quarantine-move success now decides the rebind; if the move fails the fresh registry binds to config.json.recovery-<stamp>, so no Save can clobber a file that may still hold hosts. First falsification attempt was vacuous (mutated while still locked — both behaviors pass); corrected to unlock-then-mutate (the actual data-loss window), full green-red-green cycle verified. 2 ConfigFallbackTests (corrupt-quarantine + unquarantinable-preserved), platform-forked locking (Windows exclusive handle / Unix chmod).
